name: heatmap
description: Outdoor activities heatmap.
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  mapbox_gl: ^0.16.0
  gpx: ^2.2.1
  http: ^1.1.0
  ansicolor: ^2.0.1
  path: ^1.8.3
  google_polyline_algorithm: ^3.1.0
  archive: ^3.3.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/outdoor_data/